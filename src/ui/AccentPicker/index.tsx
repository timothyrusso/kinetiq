/**
 * The accent-colour preference. Android only (see `index.android.tsx`): iOS keeps the brand
 * accent, so there is nothing to render here.
 */
export function AccentPreference(): null {
  return null;
}
