/**
 * This tab's own stack, so its root screen gets the platform's large-title header and anything
 * it pushes later keeps the tab bar underneath. The shape is shared by all three tabs.
 */
export { TabStack as default } from '@/navigation/TabStack';
